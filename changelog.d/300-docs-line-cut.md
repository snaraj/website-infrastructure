### Changed

- Cut documentation nothing reads and correct what the tree contradicts (issue #300). Delete `docs/architecture/data-flows.md` (zero inbound references; its public path, GitOps ordering and chart-selection prose each already exist in `overview.md`, `trust-boundaries.md`, ADR 0015 and ADR 0016) and `docs/assurance/evidence-ledger.schema.json` (nothing loads it; `scripts/validate_assurance_ledger.py` hardcodes every field, regex and enum, so the file was a second copy free to drift). Correct six contradictions by deletion or a shorter true sentence: `flux-install.md`'s "zero GitRepository, zero Kustomization" prestate, falsified by the live sync ceremony that operates on that GitRepository and two Ready site Kustomizations; the WARP on/off and hybrid-post-quantum launch gates in `security-control-matrix.md` and `threat-model.md`, which audit finding F1 flagged in 2026-08-10 and `practical-security-model.md` holds in its standing REJECTED set; `trust-boundaries.md`'s claim that the in-cluster storage profile shipped ENABLED, against a tree that renders no StorageClass, PersistentVolume or PersistentVolumeClaim; the 2026-08-12/18 edge observations stated as current in `edge-remediation-and-rotation.md` and `cloudflare-website-adoption.md`; the staged Cloudflare WARP administration procedure in `remote-access.md`, against a decided WireGuard/SSH-only plane; and the bounded Main Worker `PASS` receipt that `github-controls.md` still required after the owner retired it. Replace `phase-c-kubernetes-adversarial.md`'s and `image-signature-admission.md`'s restatements of `storage-admission.md` and `image-rollback.md` with pointers to them, drop the duplicated etcd-snapshot commands from `upgrades.md`, retitle `image-signature-admission.md` for the Flux and Conftest controls that exist, date `phase-a-surface-map.md`'s point-in-time State column, and replace two counts that had already rotted — seven Cloudflare phase roots where six exist, and a fixed test count in `phase-a-invariants.md`. Every doc-pinned gate anchor, byte-frozen audit and machine-rewritten line is untouched.
