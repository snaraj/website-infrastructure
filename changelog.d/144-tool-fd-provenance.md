### Security

- Hold the reviewed `kubectl` and `kustomize` bytes through every Kyverno installer invocation, closing shell-startup and helper-path injection while refusing unsafe owners, modes, links, pathname replacement, and post-binding mutation.
