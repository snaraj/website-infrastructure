### Fixed

- Recut the one-time Flux RBAC transaction for platform release `v0.1.30` in a fresh versioned state root, preserving retained prior terminal evidence while adding response-bound already-clean handling, durable fenced annotation removal, post-cleanup watcher generation evidence, and sanitized forward-failure tokens before automatic rollback.
