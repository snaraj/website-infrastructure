### Added

- Add the deploy-assurance watchdog (issue #273; the 2026-09-01 incident answer): an hourly + dispatchable workflow whose stdlib-only checker owns the release-to-domain promise with two independent conditions — site selection drift (every `kubernetes/websites/*/source.yaml` committed `chart-release` annotation compared against the latest published release of the repository named by its own cosign subject, with a selection naming an unpublished release its own worse condition) and platform publish integrity (the newest completed protected-main gate run must have a successful `Platform release` run at the same head SHA, with exactly one bounded `rerun-failed-jobs` retry for a first-attempt failure — the measured token-mint 422 recovery — and absence or repeated failure loud). Any condition turns the run red and maintains one idempotent labeled tracking issue per condition, closed with a comment when it clears; `tests/security/test_deploy_assurance_contract.py` pins the repository derivation against the real committed tree, the three-valued drift classification, the single-rerun bound, the issue reconciliation, and the workflow's narrow permission surface.
