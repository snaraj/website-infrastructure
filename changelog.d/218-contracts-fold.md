### Changed

- Fold the owner's review-ceremony reduction into `AGENTS.md`: three risk tiers govern review depth, the Main Worker Ready receipt is retired in contract (its remaining validator, skill, and template machinery named as transitional and assigned to issue #188), the repeated full-suite flake mandate becomes one author gate run plus reviewer checks with cause, and labels and process comments are stated to be coordination signals rather than control evidence. Bind reviewer independence to the posting actor — the review App that publishes the verdict — instead of a textual author/reviewer comparison, and retire that comparison from `scripts/validate_review_receipt.py` so a same-lane review the contract already permitted is representable without inventing a context string. Record the contracts wave-3 items: the `sonnet5` roster entry, the acting-agent signature generalisation, per-command SSH signing mechanics with explicit key selection, the stale-Draft re-cut doctrine, the Dependabot version-locked-pair supersede, and the `.github/dependabot.yml` delivery-lane ruling PR #127 deferred here. Publish the parallel-agent worktree contract and ignore `.claude/worktrees/` so a fresh clone carries the layout it mandates. No gate is relaxed: every fail-closed control, the owner-only merge, commit identity and signing, and the release transition gates are unchanged.
