### Security

- Accept bounded nonempty controller status-image representations without treating them as identity, restore omitted raw-list TypeMeta only from the closed Helm-owned and RBAC-binding kind/path maps, preserve pinned image and inventory/semantic bindings, and recut the closed transaction for platform release `v0.1.27`.
