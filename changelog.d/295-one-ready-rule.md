### Security

- One Ready rule (issue #295): `scripts/promote_releases.py` loses its auto-Ready machinery — the flip, the withdrawal, the compensation, the receipt and check-run reads and the label writes that fed them — so the tool's GitHub authority is exactly opening a labelled Draft, superseding its own stale one, and reporting a refused cut. The rule now lives in AGENTS.md's prose once and in `scripts/ready_check.py` once: a read-only evaluator that prints ELIGIBLE or every blocker and exits 0/3, never writing. Review is routed by risk class with severity deciding what blocks, receipts are capped at 6,000 bytes by `scripts/validate_review_receipt.py` and pull-request bodies by rule, review serializes on merged predecessors and a current base, a complexity stop compares the commission's estimate with the actuals, and the pull-request template becomes the five-field schema without the retired Main Worker rows.
