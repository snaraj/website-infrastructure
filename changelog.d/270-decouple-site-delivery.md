### Changed

- Decouple site delivery from platform releases (owner ruling 2026-09-01, issue #270): the reviewed `flux-system` GitRepository model (`kubernetes/flux-system/gotk-sync.yaml.in` and the validator's `EXPECTED_FLUX_SYNC` twin) follows protected `refs/heads/main` — the one ref a no-bypass ruleset gates, the identical anchor every cosign publisher identity already trusts — instead of a selector-advanced platform tag, so a merged site promotion deploys on the next reconcile with no platform tag, release, or selector advance; the two-directory sparse checkout, the exact-digest cosign-verified chart selection, and the receipted promotion grammar are all unchanged, and two new hostile mutants (foreign branch, reintroduced tag selector) pin the ref shape.
- Record the live half honestly instead of claiming it: merging this model change alone alters nothing on the cluster — the owner-attended ceremony in `docs/runbooks/site-sync-branch-flip.md` performs the live ref flip, suspends the `platform-release-selector` CronJob whose compare-and-swap assumes a tag ref, states verification and rollback exactly, and is the boundary after which platform releases remain the platform's own versioned audit artifact that nothing consumes for site delivery (ADR 0016 dated correction; selector README retirement notice; README desired-state row).
