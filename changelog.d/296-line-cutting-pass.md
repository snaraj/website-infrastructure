### Changed

- Cut retired narrative, unexecuted governance, and prose-pinning tests from the delivery lane (issue #296), driven by a read-only survey of every file under `scripts/ci`, `scripts/`, `tests/security` and the workflows. Kyverno was retired by issue #195; its "retired" restatements leave `AGENTS.md`, the README deployment-state table and one Flux-runbook assertion, while the lane ruling that bars recreating an admission controller and the tree-wide absence scan in `test_kyverno_retirement_contract.py` both stand unchanged. Delete the `skills/` tree and the two test modules that existed only to pin its shape, and drop its references from `AGENTS.md`, `README.md` and `.github/CODEOWNERS` — a skill never superseded `AGENTS.md`, nothing executed the tree, and every rule that leaned on a reference (the destructive-workload ledger, the vacuity-probe doctrine) is now stated where the contract states it. Remove the contract-retired `main-worker` receipt kind from `scripts/validate_review_receipt.py`, its tests, the pull-request template row and the `scripts/README.md` sentence that called it transitional, completing issue #188's machinery pass. Delete `scripts/remove_review_transaction.py`, which nothing has invoked since 2026-08-09, with its battery; delete the AST lint over the test suite itself, the duplicated CRI-postcheck regression module, and the archive and remote-access modules whose every assertion was `assertIn` over document prose. Trim eighteen further modules to the assertions that drive a subject, removing whole prose-pin tests and one duplicated textual copy rather than weakening any assertion. Drop the `syft` and `hadolint` downloads from `scripts/ci/install-tools.sh`: both were fetched on every CI and release run and invoked by nothing. Cut the four Makefile targets nothing names and `check` does not depend on. Retire the `dependabot` repository-check mode: `scripts/dependabot_contract.py` was a 643-line hand-rolled YAML parser, with a 629-line battery, gating a 17-line `.github/dependabot.yml` whose worst failure is a missed update pull request. The mode leaves the `CHECKS` registry, `validate-security.sh` and `scripts/README.md` together; this narrows what the `all` selection covers and is a deliberate scope reduction of that check, not a weakened assertion — every remaining mode still runs and still fails closed, and the mode-parity control that binds the shell's words to the registry keeps both of its anti-vacuity floors exactly at the boundary. No control, backstop, gate or rationale is removed, and every surviving assertion still has an input that turns it red.
