### Changed

- Replace shared per-PR version and changelog edits with one immutable fragment and derive each platform patch transactionally from protected tag history.
- Replace the fixed open-PR ceiling with explicit Draft dependency, collision, branch-ownership, and review-capacity controls.
