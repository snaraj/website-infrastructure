### Fixed

- Bind the one-time Flux RBAC recovery receipt to the already rolled-back
  v0.1.38 incident, admitting only the two same-chart Helm history entries
  caused by restoring the scoped site reconciler Roles.
