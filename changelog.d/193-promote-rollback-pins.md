### Changed

- Promote both sites' committed Flux image overrides off `v0.1.9`, whose tag-signed images stopped verifying once the trusted publisher identity moved to the protected `main` branch ref, forward to each site's newest published release — `naranjo-online` `v0.1.28` and `lidersea-com` `v0.1.25` — so the emergency-pin and rollback path can verify the release it actually pins. Both patches were generated by `scripts/promote-image.sh` and applied unmodified, and the suspension gates are deliberately untouched.
