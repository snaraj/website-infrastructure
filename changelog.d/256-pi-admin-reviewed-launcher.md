### Security

- Add a root-owned exact-blob launcher and transactional binary, token, service-account, systemd-unit, and runtime-redaction operations for the host-level private `pi-admin` connector. Mutable-checkout execution, tool drift, non-descendant source promotion, token disclosure, unexpected unit drop-ins, broad network-control edits, and incomplete recovery acknowledgements fail closed; package-tool refresh remains possible only through an exact two-step digest ceremony.
