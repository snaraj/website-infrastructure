### Changed

- Remove the release promoter's auto-Ready machinery: the Ready and withdrawal decisions, the write authority they needed, and the reads that fed them. `scripts/ready_check.py` now evaluates AGENTS.md's Ready rule read-only, writes nothing, and blocks on any check not finished green at the exact head; the coordinator flips and the owner merges, and adversarial-review depth scales by change class with a 6,000-byte receipt cap. Closes #295.
- Cut retired narrative and governance nothing executed: the Kyverno retirement prose (the lane ruling and the tree-wide absence scan both stand), the `skills/` tree with the two modules that only pinned its shape, the retired `main-worker` receipt kind, the 1,272-line `dependabot` validator and its battery, replaced by a 51-line closed-schema key allowlist, the unused `syft` and `hadolint` downloads, four unreferenced Makefile targets, and a long tail of tests that asserted wording rather than behaviour. Every surviving assertion still has an input that turns it red. Closes #296.
- Remove the SOPS and age scaffolding for a ceremony that never ran: `.sops.yaml`, three `bootstrap/flux` entry points, the ciphertext validator and suite, and the release-state axis they drove. Safety invariant 7 becomes "the repository carries no secrets; runtime secrets are created on the cluster by an owner ceremony", and every control is the same or stricter: any committed Kubernetes Secret manifest now fails regardless of filename, and no `flux-system` Role may grant Secret access. Closes #298, closes #148.
- Delete documentation nothing reads and correct what the tree contradicts: `docs/architecture/data-flows.md` and `docs/assurance/evidence-ledger.schema.json`, both with zero readers, plus six claims the repository falsifies, including the WARP and hybrid-post-quantum launch gates, an ENABLED storage profile that renders no volume, and a retired Main Worker receipt. Closes #300.
- Retire the Flux RBAC convergence ceremony on the owner's decision: the journaled in-place transaction, the kind acceptance harness, the custody-bound denial oracle, five test modules and the narrowing runbook. The narrowed RBAC itself is untouched: the only changes under `kubernetes/flux-system/**` are two comment blocks (the deleted runbook's link, and the SOPS read this train removed); no rule, subject, verb or resource differs. `test_flux_rbac_contract.py` still proves those manifests sufficient and narrow in both directions. Closes #299.
