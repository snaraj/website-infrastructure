### Fixed

- Validate the exact Calico runtime Pod-network annotation projection against Kubernetes Pod status during the terminal #141 recovery while continuing to reject unknown, malformed, mismatched, or behavior-changing annotations.
