### Changed

- Replace each website namespace's `capacity-not-ready` ResourceQuota, which admitted zero Pods, with the reviewed `namespace-budget` shape that `reviewed_capacity_errors` has always required. Both quotas carry the owner-selected ceilings (`6`, `150m`, `192Mi`, `1200m`, `768Mi`) and are bound to the exact SHA-256 of `docs/audits/2026-08-22-site-capacity-evidence.md`; repository and release policy now reject any other syntactically valid hash or quantity.
- Deactivate `require-zero-site-capacity.yaml` in the Kyverno policy Kustomization, moving it from the staging inventory to the promoted one. The policy file remains unchanged as the reversible source gate, while its now-orphaned report-only patch is removed and both admission render-lock stages are regenerated without changing authorization or runtime-webhook records.
- Add a closed Darwin/arm64 provenance path for offline Kyverno lock rendering with the official kustomize v5.8.1 archive and extracted-binary SHA-256 pins. `--render` binds only that platform-selected kustomize image and cannot resolve kubectl or read kubeconfig, context, server, or cluster paths; plan, apply, rollback, demote, and break-glass retain their existing fail-closed live-tool binding.
