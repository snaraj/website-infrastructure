### Changed

- Cut the delivery lane's dead code and correct its untrue comments (issue #249), the repository's first comment-level and test-level dead-content audit — the two 2026-08-10 sweeps covered `scripts/` reference reachability only. Remove ten unreachable declarations proven dead by bare-token grep and by empirical removal against the full battery: `_read_exact_single_line` in `validate_image_release.py`, `TransitionPlan.website_parent_suspended` in `validate_release_transition.py` (its only caller was deleted from `validate_repository.py`), the superseded `ACCESS_MANIFEST` in `flux_rbac_kind_acceptance.py`, the `NETWORKING_GROUP`, `GUARD_DIR` and `RECOVERY_SOURCE` constants, `Authorizer.from_paths`, the `configure_cloudflare` wrapper, and two unused imports. Delete one byte-identical duplicate test in `test_release_render_overrides.py`. Correct comments falsified by the code they annotate: the testsupport package no longer claims Flux controllers are uninstalled or cites a nonexistent ADR 0016 transition step, `test_create_release_candidate.py` no longer cites the retired `promote-image.sh` as a live precondition, `platform_bootstrap_closure.py` no longer calls itself "not repository code" while `validate_platform_bootstrap.py` exec-loads and calls it, `install-flux-controllers.sh` names the two `prune: false` per-site reconcilers it actually guards instead of a deleted `kubernetes/reconciliation` path with prune enabled, and the bootstrap floor message reports the v0.1.33 boundary its guard enforces rather than a v0.1.40 boundary it does not. Realign four counts with the currently green tests that contradict them — 306 granted atoms, 119 applied requirements, 39 declared-slack atoms including the previously undocumented 14 site-local Helm ConfigMap lifecycle grants, and 91 per-controller grant atoms — plus the six closed Cloudflare roots, five duplicated badge constants, and ten `validate_repository.py` modes of which `all` runs nine. Make both documented `validate_review_receipt.py` invocations runnable: `--author-context` became optional when the textual-difference check retired, but `--head` is required, so `AGENTS.md` and the `gh-pr-flow` skill each named a command that exits 2. Record in `docs/architecture/trust-boundaries.md` that the in-cluster media storage profile shipped enabled on 2026-08-27 by owner directive on landed evidence — Bound claim, published tree, delivery contract proven, transfer budget measured — while the public large-media delivery no-go and the chart's fail-closed values-schema conditional both stand unchanged. Amend ADR 0015 append-only for the per-site connector split that has since landed. No gate, guard, validator, or test loses the ability to go red: every removal is a declaration nothing referenced, and the `--state-absent` mutually-exclusive arms, the zero-counted `SHARED_ROLE` constant, the fail-closed `flux_rbac_denial_oracle.py` CLI surface, and the `SELF_EXEMPT` marker docstrings were each proposed, tested, and deliberately kept.
