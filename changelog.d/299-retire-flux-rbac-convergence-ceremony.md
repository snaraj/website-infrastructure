### Changed

- Retire the Flux RBAC convergence ceremony on the owner's 2026-09-02 decision (issue #299): the journaled in-place transaction and its evidence directory, the owner-run kind acceptance harness and its chart/workload fixtures, the custody-bound denial oracle, the five ceremony test modules, the `flux-rbac-kind-acceptance` Make target, and the narrowing runbook all leave. The narrowed RBAC the ceremony would have installed is unchanged: `kubernetes/flux-system/**` is byte-identical, and `tests/security/test_flux_rbac_contract.py` with `tests/security/testsupport/rbac_model.py` continue to prove those manifests sufficient and narrow in both directions. Documentation that claimed a reviewed convergence design now records that none exists and that converging the live cluster needs a fresh owner decision.
