### Fixed

- Preserve executable traversal modes on the non-root release selector's pinned Sigstore trusted-root directories, verify the actual final image root filesystem in changed-scope CI, and build a new signed selector digest whenever its reviewed immutable inputs change.
