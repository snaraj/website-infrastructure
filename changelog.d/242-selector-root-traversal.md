### Fixed

- Preserve executable traversal modes on the non-root release selector's pinned Sigstore trusted-root directories, verify the actual final image root filesystem in changed-scope CI, build a new signed selector digest whenever its reviewed immutable inputs change, and bind publication to either exact predecessor reuse or an exact protected-source rebuild.
