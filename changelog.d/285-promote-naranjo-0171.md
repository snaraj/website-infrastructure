### Changed

- Advance naranjo.online's exact signed chart selection to its current published release `0.1.71` — remediating the selection drift the deploy-assurance watchdog reported after releases `v0.1.70`/`v0.1.71` outran the issue-#281 promotion (issue #285) — by the receipted ceremony: re-resolving the reviewed tag twice with byte-identical answers, verifying the repository-at-digest against the site's `release-publisher.yml@refs/heads/main` keyless identity, inspecting the sole Helm chart layer and its embedded workload image, verifying the public SLSA v1 attestation at the exact workload index digest, and binding the protected-main source commit and immutable Release asset before the annotation and digest moved together; lidersea.com was re-verified unchanged at `0.1.41` in the same capture.
- Recapture `docs/assurance/195-chart-acquisition-receipt.{json,md}` for that acquisition, keeping every reviewed tag/digest pair identical across the receipt, `scripts/validate_signature_policy.py`, `policies/conftest/kubernetes.rego`, and the `kubernetes/websites/naranjo-online/source.yaml` selection, so no two can disagree.
