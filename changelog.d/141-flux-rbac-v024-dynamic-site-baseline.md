### Security

- Rebind the one-time protected Flux RBAC convergence transaction to exactly platform release `v0.1.24`, and derive each allowed site's canonical in-range chart version and upstream digest from its verified OCI source during planning so source/Helm mismatches and post-plan chart drift fail closed without baking moving site versions into the transaction.
