### Added

- `scripts/promote_releases.py` (issue #286): a published release opens its own reviewed promotion pull request and the owner's merge is the only human touch — promotable workloads discovered from the annotated `OCIRepository` manifests under `kubernetes/` with the acquisition profile selected by the publisher identity, the issue-195 acquisition ceremony with every judgment in code (double tag resolution with `docker-content-digest` agreement, config and sole Helm layer hash-verified by digest, `Chart.yaml`/`values.yaml` inspection, the embedded workload pin resolved twice and bound to the exact index digest, one `linux/arm64` child, cosign at the chart digest, SLSA v1 provenance at the index digest, the immutable Release asset bound to GitHub's stated digest and to the annotated tag's commit across both publisher manifest schemas), a byte-exact receipt renderer, counted rewrite of every pinned copy across the tracked tree including split string literals, a `verify` mode that must reproduce the committed receipt from the registry, and a lock-guarded launchd tick that gates, signs, pushes, opens the Draft pull request, arms both review lanes, and flips Ready only on two distinct exact-head adversarial APPROVE receipts; runbook `docs/runbooks/release-promotion.md`; the `promoter` label and `- Promoter` signature recorded in AGENTS.md.

### Changed

- The README desired-state row states the current selections as one machine-maintained sentence bound to the receipt instead of a hand-grown ladder, and the receipt Markdown is now the tool's template output at the unchanged 2026-09-01 values, so future promotions diff as values only.
