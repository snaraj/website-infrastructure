### Added

- Publish the parallel-agent worktree contract in `AGENTS.md` and ignore `.claude/worktrees/`, so a fresh clone carries the isolation, lane-ownership, shared-git-state, and cleanup rules that previously lived only in a machine-local skills folder.
