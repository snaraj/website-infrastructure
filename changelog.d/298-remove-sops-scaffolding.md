### Security

- Remove the unused SOPS scaffolding — `.sops.yaml`, the three `bootstrap/flux` age/ciphertext entry points, `validate_sops_ciphertext_snapshot.py`, the ciphertext grammar inside the repository/publication/transition validators, and the runbook and doctrine text around them — under the owner's 2026-09-02 decision that the repository carries no secrets and the Cloudflare Tunnel token is created on the cluster by an owner ceremony. No ciphertext was ever committed, and the only planned consumer stays suspended. Every remaining secret control is the same or stricter: a committed Kubernetes Secret manifest now fails regardless of filename because the `*.sops.yaml` exemption is gone, any `*.sops.ya?ml` path is a publication failure rather than an approved one, the public release Kustomization is pinned to its exact two resources so nothing can re-list a Secret, and no `flux-system` Role may grant Secret access at all in either the repository validator or the Conftest policy.
