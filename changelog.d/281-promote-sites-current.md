### Changed

- Advance both sites' exact signed chart selections to their current published releases — naranjo.online `0.1.69` and lidersea.com `0.1.41` — remediating the selection drift the deploy-assurance watchdog reported on its first scheduled run (issues #281 and #280), by the receipted ceremony: re-resolving each reviewed tag twice with identical answers, verifying each repository-at-digest against the site's `release-publisher.yml@refs/heads/main` keyless identity, inspecting the sole Helm chart layer and its embedded workload image, verifying the public SLSA v1 attestation at each exact workload index digest, and binding the protected-main source commit and immutable Release asset before each annotation and digest moved together.
- Recapture `docs/assurance/195-chart-acquisition-receipt.{json,md}` for that two-site acquisition, keeping every reviewed tag/digest pair identical across the receipt, `scripts/validate_signature_policy.py`, `policies/conftest/kubernetes.rego`, and both `kubernetes/websites/*/source.yaml` selections, so no two can disagree.
