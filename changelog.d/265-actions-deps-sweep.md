### Changed

- Bump the two GitHub Actions Dependabot proposed as PRs #264 and #263, in one release-carrying commit that supersedes both (issue #265): `docker/setup-buildx-action` 4.2.0 → 4.3.0 (`bb05f3f5519dd87d3ba754cc423b652a5edd6d2c` → `37fe631027851001ddb9b187196cc803df7f5f0e`) in the selector-image build step of `.github/workflows/platform-release.yml`, and the version-locked `github/codeql-action` pair `init` and `analyze` together 4.37.7 → 4.37.9 (`ff2f1c621b7f889edc0d3c761ac2e6a3f8cdb0dd` → `cdf488f595d80d6e07e03d4674febd5ab45fa938`) in `.github/workflows/codeql.yml`. Every pin was proven against the upstream repository rather than trusted from the bot's diff: each new SHA is the exact commit its claimed tag dereferences to, and each outgoing SHA is the exact commit the outgoing tag dereferences to, so the version comments and the SHAs cannot silently disagree. Upstream carries no behavioral change this workflow depends on — 4.3.0 is five bundled npm dependency bumps in the action's own tree, 4.37.8 is "no user facing changes", and 4.37.9 only advances the default CodeQL bundle to 2.26.4, which this repository does not pin.
- Lift the two closed step inventories in `tests/security/test_platform_release_contract.py` that named the outgoing Buildx SHA, so the assertion keeps proving the selector-image transaction still builds through a SHA-pinned Buildx step rather than freezing one particular release of it. Nothing else moved: `.github/dependabot.yml` needs no change because its `groups.codeql-action` stanza already did its job — #263 arrived with both halves of the locked pair in one PR — and `docker/setup-buildx-action` has no version-locked sibling here. The `CodeQlActionLockstepTests` battery, which detects a split arriving from a manual edit rather than from the bot, is untouched and still passes at the new pin.
