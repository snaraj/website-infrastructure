# SR-10 in isolation. Enumerated name, enumerated provisioner, and a remote
# endpoint smuggled into parameters — so this fixture also needs the enumerated
# class name and its own resource set.
apiVersion: cli.kyverno.io/v1alpha1
kind: Test
metadata:
  name: storage-class-parameter-boundary
policies:
  - ../../../../policies/kyverno/disallow-undiscovered-storage.yaml
resources:
  - ../../fixtures/deny/storage-class-with-parameters.yaml
results:
  - policy: disallow-undiscovered-storage
    rule: disallow-persistent-storage-resources
    resources: [local-pie-ssd]
    kind: StorageClass
    result: fail
