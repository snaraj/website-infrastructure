# SR-0. The document parses as YAML but the spec is a scalar, not a mapping, so
# no field access can succeed. A rule that silently becomes undefined on such an
# object is an allow; this one denies explicitly.
apiVersion: v1
kind: PersistentVolume
metadata:
  name: storage-unstructured
spec: not-a-mapping
