# Current-main's ImageReferenceGrammarTests pins this exact Conftest denial set:
# a floating tag plus digest violates the site's closed canonical grammar only.
container image-floating-tag must use the canonical image repository for namespace naranjo-online
