# Current-main's TOKEN_BINDING_DENY_CORPUS pins this file to the ephemeral
# container surface. Its sole attribution is the positive env binding.
Pod cloudflare-public/connector-ephemeral-cross-site may take its Tunnel token only from the Secret derived from its own app.kubernetes.io/instance, through env.valueFrom.secretKeyRef and never through envFrom
