# Current-main's ImageReferenceGrammarTests pins this exact Conftest denial set:
# an unprefixed release tag plus digest violates the canonical grammar only.
container image-unprefixed-release-tag must use the canonical image repository for namespace naranjo-online
