# STRUCTURAL EXAMPLE ONLY. This path is not included by any Kustomization.
# The repository carries no secrets: the real Secret is created on the cluster
# by the owner's tunnel-token ceremony and never committed here.
apiVersion: v1
kind: Secret
metadata:
  name: EXAMPLE-DO-NOT-APPLY
  namespace: cloudflare-public
type: Opaque
stringData:
  token: NOT_A_REAL_TOKEN_AND_INTENTIONALLY_UNUSABLE
