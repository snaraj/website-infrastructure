apiVersion: v1
kind: ServiceAccount
metadata:
  name: lidersea-com
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "lidersea-com.labels" . | nindent 4 }}
# The static origin never calls the Kubernetes API, so no projected credential
# should enter its pod even if a future default changes.
automountServiceAccountToken: false
