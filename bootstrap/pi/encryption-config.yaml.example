# Never replace these sentinels by hand or print key material in a terminal.
# The generator entrypoint is code-blocked until the separately installed
# reviewed-blob launcher exists. Do not replace these sentinels before then.
apiVersion: apiserver.config.k8s.io/v1
kind: EncryptionConfiguration
resources:
  - resources:
      - secrets
    providers:
      - secretbox:
          keys:
            - name: REPLACE_KEY_NAME
              secret: REPLACE_BASE64_32_BYTE_KEY
      - identity: {}
