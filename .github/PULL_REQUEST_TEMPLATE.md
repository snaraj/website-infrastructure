<!-- Five fields. Delete nothing; write "none" where empty. What a reviewer
checked belongs in the receipt, not here. -->

## Head and base

- Exact base: `main` @ <!-- sha -->
- Exact head: <!-- 40-hex sha; update after every author push -->

## Scope

Closes #<!-- same-repository issue number -->

<!-- What changed; the dependency order (predecessors that must merge first);
the actuals against the issue's estimate (files, net lines, review rounds);
any path the Delivery lane rulings leave unassigned, declared as a crossing;
the one new changelog.d fragment. -->

## Gates

<!-- One row per command with the figure it produced. -->

## Findings resolved

<!-- One line per round: round -> what changed. -->

## Limitations

<!-- Residual risk, rollback, and anything a plain `git revert` misses. -->
